#include "main.h"
#include <limits.h>
int main(void)
{
	unsigned int ui = ui = (unsigned int)INT_MAX + 1024;

	_printf("Unsigned:[%u]\n", ui);
	_printf("my dog has a puppy%sonly", " ");
	return (0);
}
